import { Button } from '@packages/uiKit';
import styled from 'styled-components';

export const StyledFilterResetButton = styled(Button).attrs({
  variant: 'secondary-outlined',
})`
  align-self: flex-start;
`;
