import { Typography } from '@packages/uiKit';
import styled from 'styled-components';

export const StyledRestorePasswordRequestModal = styled.div``;

export const StyledDescription = styled(Typography).attrs({
  size: 16,
  weight: 400,
  color: 'gray18',
})`
  margin-bottom: 31px;
`;
